module.exports = {
  plugins: [
    require("tailwindcss"),
    require("autoprefixer"),
    // require('@fullhuman/postcss-purgecss')({
    //   content: ['./index.html', './projects.html', './src/main.ts'],
    //   defaultExtractor: (content) => content.match(/[^<>"'`\s]*[^<>"'`\s:]/g),
    // }),
  ],
};
